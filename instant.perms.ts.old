const INSTANT_APP_ID = 'af77353a-0a48-455f-b892-010232a052b4';
export default {
    $files: {
        allow: {
            view: "auth.id != null",  // Authenticated users can view files
            create: "auth.id != null" // Authenticated users can upload files
        }
    },
  };